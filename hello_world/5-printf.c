#include<stdio.h>
#include<stdlib.h>
/**
 *  * main -Entry point
 *   * Return: Always 0 (Success)
 *    */

int main(void)
{
	const char* str="with proper grammar, but the outcome is a piece of art,";
		printf("%s \n", str1);
		
		return (0);
}
