#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * _islower - Entry point
 */
int _islower(void)
{
	char c;

	if (c (is lowercase))
	{
		printf("%c _islower(): %d", c, _islower(c));
		return (1);
	}


	return (0);
}
